name: python application

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - run: ls -la
      - run: pwd
      - run: touch gitb
      - name: list
        run: ls
      
